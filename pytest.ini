[pytest]
# Custom markers
markers =
    integration: marks tests that require live LLM API keys and a running workflow
    local_only: marks unit tests that depend on specific local environment configuration (deselect with -m "not local_only" in CI)

# Default test paths
testpaths = tests
